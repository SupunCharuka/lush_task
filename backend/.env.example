# Server
PORT=5000
MONGODB_URL=mongodb+srv://root:password@cluster.example.mongodb.net/lush-task

# SMTP (example using SMTP server)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-smtp-password
FROM_EMAIL="My Company <no-reply@example.com>"

# Notes:
# - For Gmail SMTP use smtp.gmail.com and enable app passwords or OAuth (less secure apps is deprecated).
# - Keep credentials secret. Copy this file to `.env` and fill real values.
